name: expense_tracker
description: A clean architecture Flutter project for tracking expenses.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  sqflite: ^2.3.0
  path_provider: ^2.0.15
  path: ^1.8.3
  http: ^1.4.0
  intl: ^0.18.1
  sqflite_common_ffi: ^2.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
